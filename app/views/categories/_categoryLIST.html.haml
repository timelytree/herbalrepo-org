#categoryLIST.box
  - @categories.each do |category|
    %a{href: "/category/#{category.slug}", class: "categoryTILE #{session[:current_category] == category.name.downcase ? 'active' : ''}", data:{category: category.name.downcase}}
      %div.flex-c
        %img{src: "../images/icons/#{strip_string(category.name.downcase)}_icon_black.svg"}
        %p= category.name
