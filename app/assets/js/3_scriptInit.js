/////////////////////////////////////////////////////// initialization functions
//------------------------------------------------------------------------------
function globalInit() {
  run(init.global.ajaxSetup);
}

function desktop() {
  run(core.desktop.herbPanelINT);
  switch (p.Current) {
  }
}

function mobile() {
  switch (p.Current) {
  }
}
